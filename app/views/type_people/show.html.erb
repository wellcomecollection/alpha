<% content_for :title, "#{@person.name} within #{@type.name}" %>

<h6><%= link_to 'Types', types_path %> &rarr; <%= link_to @type.name, @type %> / <%= link_to "People", type_people_path(@type) %> /</h6>

<h1><%= link_to @person.name, @person %></h2>

<% @records.each do |record| %>
  <%= render record %>
<% end %>