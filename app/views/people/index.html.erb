<h1>People</h1>

  <% @top_people.each do |person| %>

  <div class="circle-container">
    <div class="portrait-circle"></div>
    <div class="clicky" style="width:100%;height:100%;background-color:pink;"><%= link_to person.name, person %></div>
    <div class="name" title="<%= pluralize(person.records_count, 'thing') %>">
      <%= link_to person.name, person %><br />
      <small>subject1, subject2</small>
    </div>
  </div>

  <% end %>


<br clear="all" />
<br clear="all" />
<br clear="all" />
<br clear="all" />
<ol>
  <% @top_people.each do |person| %>
    <li><%= link_to person.name, person %> <small><%= pluralize(person.records_count, 'thing') %></small></li>
  <% end %>
</ol>
