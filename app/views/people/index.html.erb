<h1>People</h1>

  <% @top_people.each do |person| %>

  <div class="circle">
    <%= link_to person.name, person, class: 'bg-link', title: pluralize(person.records_count, 'thing') %>
    <%= link_to person.name, person, class: 'name', title: pluralize(person.records_count, 'thing') %>
  </div>

  <% end %>


<br clear="all" />
<br clear="all" />
<br clear="all" />
<br clear="all" />
<ol>
  <% @top_people.each do |person| %>
    <li><%= link_to person.name, person %> <small><%= pluralize(person.records_count, 'thing') %></small></li>
  <% end %>
</ol>
